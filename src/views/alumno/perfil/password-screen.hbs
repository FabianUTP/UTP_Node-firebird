<section class="section profile">
  <div class="pb-3">
    {{>btnBack}}

    <!-- Mostrar mensaje de éxito o error si existe -->
    {{#if message}}
      <div class="alert alert-success" role="alert">
        {{message}}
      </div>
    {{/if}}
  </div>

  <div class="col-xl-9">
    <div class="card">
      <div class="card-header">
        <h4>Mi Perfil</h4>
      </div>
      <div class="card-body pt-3">
        <!-- Bordered Tabs -->
        <ul class="nav nav-pills mb-4" id="pills-tab" role="tablist">
          <li class="nav-item">
            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#datos">Identidad</button>
          </li>
          <li class="nav-item">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#contacto">Cambiar Contraseña</button>
          </li>
        </ul>
        <div class="tab-content pt-3">
          <!-- Datos de Identidad -->
          <div class="tab-pane fade show active profile-overview" id="datos">
            <div class="row justify-content-center mb-3">
              <div class="col-lg-6 col-md-8">
                <img src="{{image}}" alt="foto" class="img-fluid profile-image" width="250">
              </div>
            </div>
            <div class="profile-info">
              <div class="row">
                <div class="col-lg-3 col-md-4 label">NOMBRE</div>
                <div class="col-lg-9 col-md-8">{{NOMBRE}}</div>
              </div>
              <!-- Más información de identidad... -->
            </div>
          </div>

          <!-- Cambiar Contraseña -->
          <div class="tab-pane fade pt-3" id="contacto">
            <form method="post" action="/passworddata/update-contactos">
              <div class="row mb-3">
                <label for="alumno_password" class="col-md-4 col-lg-3 col-form-label">Contraseña</label>
                <div class="col-md-8 col-lg-9">
                  <div class="input-group">
                    <input name="alumno_password" type="password" class="form-control" id="alumno_password" value="{{ALUMNO_PASSWORD}}" required />
                    <button class="btn btn-outline-secondary" type="button" id="togglePassword">👁</button>
                  </div>
                </div>
              </div>
              <div class="text-center">
                <button class="btn btn-outline-primary" type="submit">Guardar</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.getElementById('togglePassword').addEventListener('click', function () {
    const passwordField = document.getElementById('alumno_password');
    if (passwordField.type === 'password') {
      passwordField.type = 'text';
    } else {
      passwordField.type = 'password';
    }
  });
</script>

<script src="/js/services/Perfil_Screen.js"></script>

<style>
  body {
    background-color: #f4f6f9; /* Fondo gris claro */
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    margin: 0;
    padding: 0;
  }

  .section.profile {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 40px;
  }

  .pb-3 {
    width: 100%;
    text-align: left;
  }

  .card {
    width: 100%;
    max-width: 800px;
    background: #fff;
    border: none;
    border-radius: 16px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }

  .card-header {
    background-color: #911820;
    color: white;
    font-size: 20px;
    font-weight: bold;
    padding: 20px;
    border-radius: 16px 16px 0 0;
  }

  .card-body {
    padding: 30px;
  }

  .nav-pills .nav-link {
    background-color: #911820;
    color: white;
    border-radius: 0; /* Sin bordes redondeados */
    padding: 12px 25px;
    font-size: 14px;
    transition: background-color 0.3s ease;
  }

  .nav-pills .nav-link.active {
    background-color: #911820;
  }

  .profile-info {
    margin-top: 20px;
  }

  .profile-info .label {
    font-weight: bold;
    color: #911820;
  }

  .profile-info .row {
    margin-bottom: 15px;
  }

  .profile-image {
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }

  .input-group input {
    border-radius: 0; /* Sin bordes redondeados */
    padding: 12px 20px;
    width: 100%;
  }

  .input-group button {
    background-color: #911820;
    color: white;
    border: none;
    border-radius: 0; /* Sin bordes redondeados */
    padding: 12px 20px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .input-group button:hover {
    background-color: #911820;
  }

  .text-center .btn-outline-primary {
    background-color: #911820;
    color: white;
    border: none;
    padding: 12px 24px;
    font-size: 16px;
    border-radius: 0; /* Sin bordes redondeados */
    transition: background-color 0.3s ease;
  }

  .text-center .btn-outline-primary:hover {
    background-color: #911820;
  }

  .alert {
    margin-top: 20px;
    border-radius: 8px;
    padding: 15px;
    background-color: #d4edda; /* Fondo verde claro para mensajes de éxito */
    color: #155724; /* Texto verde oscuro */
  }

  .alert-error {
    background-color: #f8d7da; /* Fondo rojo claro para mensajes de error */
    color: #721c24; /* Texto rojo oscuro */
  }
</style>
