
<head>
<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f0f0f0;
    }

    #mensaje {
        display: none;
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.3);
        padding:100px;
        text-align: center;
        max-width: 500px;
    }

    h1 {
        font-size: 34px;
        margin-bottom: 15px;
        color: #333;
    }

    p {
        font-size: 18px;
        color: #666;
    }

    #cerrarMensaje {
        background-color: #ff6600;
        color: #fff;
        border: none;
        padding: 10px 20px;
        margin-top: 20px;
        cursor: pointer;
        border-radius: 5px;
        font-size: 16px;
    }

    #cerrarMensaje:hover {
        background-color: #ff8533;
    }
</style>
</head>
<body>
    <div id="mensaje">
        <h1>Tu inscripción ha sido registrada</h1>
        <p>¡Gracias por tu registro!</p>
        <button id="cerrarMensaje">Cerrar</button>
    </div>

    <script>
        // Función para ocultar el mensaje emergente y redirigir a la vista principal
        function cerrarMensaje() {
            var mensaje = document.getElementById("mensaje");
            mensaje.style.display = "none";

            // Redirige a la vista principal (puedes cambiar la URL a la que desees redirigir)
            window.location.href = "./";
        }

        // Muestra el mensaje al cargar la página
        window.onload = function() {
            var mensaje = document.getElementById("mensaje");
            mensaje.style.display = "block";
        }

        // Asigna evento al botón de cerrar
        var cerrarBoton = document.getElementById("cerrarMensaje");
        cerrarBoton.addEventListener("click", cerrarMensaje);
    </script>
</body>

